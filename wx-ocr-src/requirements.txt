--extra-index-url https://download.pytorch.org/whl/cu126

torch
torchvision
torchaudio

pymupdf
pymupdf4llm
marker-pdf

# VLM engine: Qwen2.5-VL-3B-Instruct-AWQ (loaded from repos-folder by default)
# transformers must be >=4.49 for the Qwen2_5_VL model class.
transformers>=4.49
accelerate
autoawq

# MinerU pipeline engine — layout + OCR + formula + table, no VLM.
# Pipeline extras pull torch/torchvision/transformers/onnxruntime/etc.
# Coexists with Qwen2.5-VL-AWQ (no >=4.57 transformers bump from this extra).
mineru[pipeline]
