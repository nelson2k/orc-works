--extra-index-url https://download.pytorch.org/whl/cu126

# Stage 1: install before stage 2.
# Marker-pdf pins Pillow<11.0.0; MinerU pins Pillow>=11.0.0. pip's strict
# resolver refuses to satisfy both in one pass, so we install marker first
# (with its preferred old Pillow) and let stage 2 bump Pillow afterwards.

torch
torchvision
torchaudio

pymupdf
pymupdf4llm
marker-pdf

# VLM engine: Qwen2.5-VL-3B-Instruct-AWQ (loaded from repos-folder by default)
# transformers must be >=4.49 for the Qwen2_5_VL model class.
transformers>=4.49
accelerate
autoawq
